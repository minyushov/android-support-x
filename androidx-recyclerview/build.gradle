apply from: "${rootDir}/android-library.gradle"

dependencies {
  api androidRecyclerView
  testImplementation "androidx.test:rules:1.2.0"
  testImplementation "junit:junit:4.12"
}

bintrayUpload {
  artifactId = "recyclerview"
  version = "2.0.0"
}